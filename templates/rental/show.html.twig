{% extends 'base.html.twig' %}

{% block title %}Rental{% endblock %}

{% block body %}
    <h1>Rental</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rental.id }}</td>
            </tr>
            <tr>
                <th>CarRentalDate</th>
                <td>{{ rental.carRentalDate ? rental.carRentalDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>CarRentalReturnDate</th>
                <td>{{ rental.carRentalReturnDate ? rental.carRentalReturnDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rental_index') }}">back to list</a>

    <a href="{{ path('rental_edit', {'id': rental.id}) }}">edit</a>

    {{ include('rental/_delete_form.html.twig') }}
{% endblock %}
