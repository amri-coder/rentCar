{% extends 'base.html.twig' %}

{% block title %}Edit Engines{% endblock %}

{% block body %}
    <h1>Edit Engines</h1>

    {{ include('engines/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('engines_index') }}">back to list</a>

    {{ include('engines/_delete_form.html.twig') }}
{% endblock %}
