<form method="post" action="{{ path('gears_delete', {'id': gear.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gear.id) }}">
    <button class="btn">Delete</button>
</form>
